<% content_for :title, "New OAuth app · Iron Control" %>

<div class="mb-8">
  <a href="<%= console_oauth_apps_path %>" class="text-xs text-zinc-500 hover:text-iron-400">← back to OAuth apps</a>
  <div class="mt-2 flex flex-wrap items-center gap-3">
    <h1 class="text-2xl font-bold text-zinc-100">New OAuth app</h1>
    <span class="rounded-md bg-iron-500/10 px-2 py-0.5 text-xs font-medium text-iron-300 ring-1 ring-iron-500/25">OAuth</span>
  </div>
</div>

<%= render "errors", app: @app %>
<%= render "form", app: @app %>
