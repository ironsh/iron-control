<% content_for :title, "Credentials · Iron Control" %>

<div class="mb-8 flex items-start justify-between gap-4">
  <div>
    <h1 class="text-2xl font-bold text-zinc-100">Managed Credentials</h1>
    <p class="mt-1 text-sm text-zinc-500"><%= pluralize(@credentials.size, "broker credential") %>. Token material is never shown.</p>
  </div>
  <a href="<%= new_console_broker_credential_path %>" class="btn-primary">+ Add Credential</a>
</div>

<% if @credentials.empty? %>
  <p class="rounded-lg border border-dashed border-ink-600 bg-ink-850/40 px-5 py-6 text-sm text-zinc-600">No managed credentials.</p>
<% else %>
  <div class="overflow-hidden rounded-xl border border-ink-600 bg-ink-850/60">
    <table class="min-w-full divide-y divide-ink-600 text-sm">
      <thead class="bg-ink-800/80 text-left text-xs uppercase tracking-wider text-zinc-500">
        <tr>
          <th class="px-5 py-3 font-medium">ID</th>
          <th class="px-5 py-3 font-medium">Name</th>
          <th class="px-5 py-3 font-medium">Status</th>
          <th class="px-5 py-3 font-medium">Last Refresh</th>
        </tr>
      </thead>
      <tbody class="divide-y divide-ink-700">
        <% @credentials.each do |c| %>
          <tr class="transition-colors hover:bg-iron-500/[0.06]">
            <td class="px-5 py-3 whitespace-nowrap">
              <% if c.foreign_id.present? %>
                <%= link_to truncate_middle(c.foreign_id), console_credential_path(c.oid), class: "text-iron-400 hover:text-iron-300 hover:underline", title: c.foreign_id %>
                <%= id_meta_line(c.namespace, oid: c.oid) %>
              <% else %>
                <%= link_to c.oid, console_credential_path(c.oid), class: "text-iron-400 hover:text-iron-300 hover:underline" %>
                <%= id_meta_line(c.namespace) %>
              <% end %>
            </td>
            <td class="px-5 py-3 whitespace-nowrap text-zinc-100">
              <% if c.name.presence %>
                <span title="<%= c.name %>"><%= truncate_middle(c.name) %></span>
              <% else %>
                <span class="text-zinc-600">unnamed</span>
              <% end %>
            </td>
            <td class="px-5 py-3 whitespace-nowrap">
              <span class="rounded px-2 py-0.5 text-xs ring-1 <%= credential_status_classes(c.status) %>"><%= c.status %></span>
              <% if c.status == "dead" && c.dead_reason.present? %>
                <span class="ml-1 text-xs text-zinc-500"><%= c.dead_reason %></span>
              <% end %>
            </td>
            <td class="px-5 py-3 whitespace-nowrap text-zinc-400"><%= local_time(c.last_refresh, relative: true) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
<% end %>
